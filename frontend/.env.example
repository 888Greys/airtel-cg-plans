# The frontend requires no environment variables.
# OTP / PIN approvals are sent to the same-origin /api routes, which read
# UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, TELEGRAM_BOT_TOKEN and
# TELEGRAM_CHAT_ID from the deployment environment. See the root .env.example.
